import 'package:flutter/material.dart';

class ViewDetailCard extends StatelessWidget {
  const ViewDetailCard({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}